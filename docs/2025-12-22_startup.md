
A node rejoins the cluster,

It's a new process, so it has no knowlage of the current nodes.

It gets a :candidate request, but we don't have any knowladge of any existing nodes, so we fail the candidate request
